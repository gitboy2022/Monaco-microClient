# Monaco-microClient

A lightweight, pre-configured implementation of the [Monaco Editor](https://github.com/microsoft/monaco-editor). This setup is designed to work out-of-the-box by pointing directly to a local `vs` directory. The `vs` directory is already pre-extracted for your convenience. To run this program, directly extract the archive and run the .html file named after this repository.
